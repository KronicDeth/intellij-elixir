package org.elixir_lang.psi.call.arguments;

import org.elixir_lang.psi.call.Named;

/**
 * A call with zero or more arguments (like {@code *} in a regular expression).
 */
public interface Star extends Named {
}
